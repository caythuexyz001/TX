# Database (SQLite path). Railway ephemeral FS resets; for persistence use a Railway Volume or hosted DB.
DB_DSN=sqlite:///./data/tai_xiu.db
# Window & thresholds
WINDOW=50
ALPHA_WARN=0.10
THETA=0.10
# Optional API key for write endpoints
API_KEY=01279992564a
